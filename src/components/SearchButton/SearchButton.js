import React from 'react';
import './SearchButton.css';


const SearchButton = (props) => {
    return (        
        <button className="margin-5">Search</button>
    )
};

export default SearchButton;